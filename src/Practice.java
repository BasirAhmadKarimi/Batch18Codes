public class Practice {
    public static void main(String[] args) {
       /* String userName="adfdskhf";
        String password="pass1234";
        String confirm="pass1234";

        if(userName.isEmpty() ||password.isEmpty()){
            System.out.println("username and password can not be empty");

        }else if (password.length()<8){
            System.out.println("password is too short");
        }else if (password.contains(userName)){
            System.out.println( "password cannot contain username");
        }else if (!password.equals(confirm)){
            System.out.println("password do not match");
        }else{
            System.out.println("your usernam and password has been created");
        }

        String str="Java is fun";
        char c= str.charAt(5);
        for (int i = 0; i < str.length(); i++) {
            System.out.print(str.charAt(i)+" ");

        }

        String str="Java is fun";
        int count=0;
        for (int i = 0; i < str.length(); i++) {
          if (str.charAt(i)=='a'){
              count++;
          }

        }
        System.out.println(count);*/


        String str1 = "Hello ";
        if (!str1.isEmpty()) {
            int len = str1.length();
            if (len >= 3 && len % 2 != 0) {

            }
            System.out.println(str1.charAt(len / 2));
        }
           }
       }



